input = load('6pointsinputs.txt');
output = load('6pointsoutputs.txt');


weights = zeros(6,3);
iterations = zeros(6,1);

for i=1:6
    [weights(i,:), iterations(i)] = perceptron(input,output);
end

%test each of the weight vectors agains all inputs
for j=1:6
    sign = dot(weig
    
end