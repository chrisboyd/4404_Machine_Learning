x = load('dataset1_inputs.txt');
t = load('dataset1_outputs.txt');
w = 30;
errors = zeros(1,w);
poly = designmatrix(x,t,10);
p = polyfit(x,t,10);
poly = poly';
res = evalpoly(poly,x);
real = polyval(p,x);